<h1>The fakest fake news around!</h1>
<%= link_to "Submit a new article", new_article_url %>



<% @articles.each do |article| %>
  <h3><%= article.title %></h3>
  <%= link_to "See more...", article_path(article) %>
<% end %>
